from pydantic import BaseModel

class {{ agent_name }}(BaseModel):
    systemprompt: str = "{{ systemprompt }}"
    {% for key, value in properties.items() %}
    {{ key }}: str = "{{ value }}"
    {% endfor %}

    def run(self):
        # Hier kann die Agent-Logik implementiert werden
        print(f"Agent '{{ agent_name }}' gestartet mit Systemprompt: {{ systemprompt }}")